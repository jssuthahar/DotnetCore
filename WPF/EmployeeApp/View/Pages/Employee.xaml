<Page x:Class="EmployeeApp.View.Pages.Employee"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Background="#F3F6FB"
      Title="Client">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="Register New Client"
                       FontSize="24"
                       FontWeight="Bold"
                       Foreground="#2E3B55"
                       Margin="0 0 0 20"/>

            <!-- Form Layout -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Top" >
                    <Label Content="Client Name" Margin="0,0,0,10"/>
                    <Label Content="Email" Margin="0,0,0,10"/>
                    <Label Content="Phone" Margin="0,0,0,10"/>
                    <Label Content="Company Name" Margin="0,0,0,10"/>
                    <Label Content="GST Number" Margin="0,0,0,10"/>
                    <Label Content="Address" Margin="0,0,0,10"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Top" >
                    <TextBox x:Name="txtClientName" Height="30" Margin="0,0,0,10"/>
                    <TextBox x:Name="txtClientEmail" Height="30" Margin="0,0,0,10"/>
                    <TextBox x:Name="txtClientPhone" Height="30" Margin="0,0,0,10"/>
                    <TextBox x:Name="txtCompany" Height="30" Margin="0,0,0,10"/>
                    <TextBox x:Name="txtGST" Height="30" Margin="0,0,0,10"/>
                    <TextBox x:Name="txtClientAddress" Height="60" AcceptsReturn="True" TextWrapping="Wrap" Margin="0,0,0,10"/>
                </StackPanel>
            </Grid>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 20 0 0">
                <Button Content="Clear" Width="100" Margin="0 0 10 0" />
                <Button Content="Register" Width="100" Background="#2E3B55" Foreground="White" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
